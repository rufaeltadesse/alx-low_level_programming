#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _memset - entry
 * @s: is a variabl
 * @b: is a variable
 * @n: is avariable
 * Return: 0
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}
