#include "main.h"
#include <string.h>
/**
 * _strcpy - entry
 * @dest: is avariable
 * @src: is avariable
 * Return: 0
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
