#include "main.h"
#include <string.h>
/**
 * _strcat - entry
 * @dest: is a variable
 * @src: is a variable
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	strcat(dest, src);
	return (dest);
}
