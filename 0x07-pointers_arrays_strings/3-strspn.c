#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * strspn - entry
 * @s: is a variable
 * @accept: is a variable
 * Return: 0
 */
unsigned int _strspn(char *s, char *accept)
{
	return (strspn(s, accept));
}
